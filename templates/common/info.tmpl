{{template "header"}}
<div>
	<img src="/img/{{.filename}}" width="500px"></img>
	<ul>
		{{.filename}}<br/>
		{{.directory}}<br/>
		{{.size}}<br/>
		{{.type}}<br/>
		{{.overwriten}}<br/>
	</ul>
</div>
{{template "footer"}}
